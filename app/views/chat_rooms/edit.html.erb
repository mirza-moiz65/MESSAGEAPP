<h1>Editing Chat Room</h1>

<%= render 'form', {chat_room: @chat_room, url: chat_room_path(@chat_room), method: :patch} %>
<td><%= link_to 'Show', chat_room_path(id: @chat_room.id) %></td>
<%= link_to 'Back', chat_rooms_path %>
