<p id="notice"><%= notice %></p>

<h1>Chat Rooms</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @chat_rooms.each do |chat_room| %>
      <tr>
        <td><%= chat_room.name %></td>
        <td><%= link_to 'Show', chat_room_path(id: chat_room.id) %></td>
        <td><%= link_to 'Edit', edit_chat_room_path(id: chat_room.id)%></td> 
        <%# <td><%= link_to 'Destroy', user_chat_room_url, method: :delete, data: { confirm: 'Are you sure?' }</td>  %> 
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Chat Room', new_chat_room_path %>
